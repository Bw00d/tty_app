<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: @micropost %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Compose..." %>
	</div>
	<%= f.submit "Post", class: "btn btn-lrg btn-primary" %>
	<% end %>